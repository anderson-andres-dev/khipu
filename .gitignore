# Rust workspace (cargo builds every member into this shared dir)
/target/

# Editor / OS
.DS_Store
.idea/
*.swp

# graphify knowledge graph (generated, regenerate with `graphify update .`)
/graphify-out/
